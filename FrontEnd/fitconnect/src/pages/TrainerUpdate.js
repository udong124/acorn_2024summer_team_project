import React from 'react';

function TrainerUpdate(props) {
    return (
        <div>
            
        </div>
    );
}

// 이 컴포넌트는 trainer만 접근 가능하도록 설정
TrainerUpdate.allowedRoles = ['trainer'];

export default TrainerUpdate;