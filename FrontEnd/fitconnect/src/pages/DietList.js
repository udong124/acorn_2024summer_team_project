import React from 'react';

function DietList(props) {
    return (
        <div>
            
        </div>
    );
}

// 이 컴포넌트는 member만 접근 가능하도록 설정
DietList.allowedRoles = ['member'];

export default DietList;